## node js is runtime 
JS runs outside the browser
v8
## npm - node package manager

% npm install -D typescript tsx @types/node

## Depenedencies?
- Dependencies is needed when the app is running
## Dev-Dependencies?
- Dev-Dependencies is needed while developing app

runtime package - 

folder, file => holding any responsbility
uitls => can not any routing
mixing => service logic and db logic
routing => service logic (BAD Practice)

always have one entry file or one root file
root file => should have the information of your entire source code